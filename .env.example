# Backend server port
SERVER_PORT=3000

# Allowed origin(s) for CORS / Socket.IO
# Comma-separated list of origins the client is served from
CLIENT_URL=http://localhost:5173

# URL the client uses to connect to the Socket.IO server
VITE_SOCKET_URL=http://localhost:3000
